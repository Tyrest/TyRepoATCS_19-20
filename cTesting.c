#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv)
{
    // unsigned char * divinePointress = (unsigned char *) malloc(69 * sizeof(unsigned char));
    // unsigned char * furthestMember = divinePointress;

    // printf("%d", divinePointress - furthestMember);

    // free(divinePointress);

    // unsigned char zero = 0;
    // unsigned char * divinePointress = &zero;
    // *divinePointress -= 1;
    // printf("%d", zero);

    // unsigned char testChar = 33;
    // unsigned char * testPtr = &testChar;
    // printf("Test Char: %c\n", *testPtr);
    // printf("Test Int: %d\n", *testPtr);
    // printf("Test Add: %d\n", *testPtr + 10);
    // printf("Test Inc: %d\n", *testPtr++);

    int z = 0;
    while (z == 0)
    {
        printf("Hello");
        z = 1;
    }
}